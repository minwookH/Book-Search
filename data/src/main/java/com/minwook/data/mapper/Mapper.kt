package com.minwook.data.mapper


/*
fun SearchBookResponse.toData() : Banner {
    return Banner(
        title = title,
        description = description,
        imageUrl = imageUrl
    )
}

fun List<ProgramResponse>.toListData() : List<Program> {
    return this.map {
        it.toData()
    }
}

fun ProgramResponse.toData(badgeStatus: String = "") : Program {
    return Program(
        id = id,
        imageUrl = imageUrl,
        level = level,
        averageMinute = averageMinute,
        effect = effect,
        coach = coach,
        category = category,
        title = title,
        badgeStatus = badgeStatus
    )
}*/
